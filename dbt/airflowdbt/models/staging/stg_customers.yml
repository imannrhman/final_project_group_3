models:
  - name: stg_customers
    description: Coupons data with basic cleaning and transformation applied, one row per customer.
    columns:
      - name: customers_id
        description: The unique key for each customer.
        tests:
          - not_null
          - unique